/**
 * Pi extension entry point — re-exports the built attractor extension
 * so that `/reload` auto-discovers it from `.pi/extensions/`.
 */
export { default } from "../../dist/extensions/attractor.js";
